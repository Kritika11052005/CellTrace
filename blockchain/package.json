{
    "name": "battery-blockchain",
    "version": "1.0.0",
    "description": "Battery provenance smart contract — hackathon build",
    "scripts": {
        "compile": "hardhat compile",
        "deploy:amoy": "hardhat run scripts/deploy.js --network amoy"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^3.0.0",
        "dotenv": "^16.4.5",
        "hardhat": "^2.22.0"
    }
}